# sample-audit-runner
GHA runner for sample-library audit (opus-based per-zone QC). Reads manifests from zulfikarbarbora-outl/web-daw-samples, downloads opus previews from public adc-* repos, writes results back to web-daw-samples/audit-results/.
